# 慢慢书桌 · 飞书统计接入

当前状态：前端事件逻辑已实现；飞书接收端、记录表和仪表盘尚未建立；线上数据采集未启用。`analytics-config.js` 的 endpoint 保持空字符串。

## 第一版口径

| 事件 | 触发方式 | 用途 |
|---|---|---|
| page_view | 开启统计后，每次页面载入一次 | 打开次数与时间 |
| play_start | 选中音乐实际开始播放且应用音量大于零 | 有效开始播放 |
| listen_30s | 同一次选曲累计有效媒体进度达到 30 秒 | 有效收听 |

一首歌曲暂停后恢复不重复计播放开始，同一选曲循环不重复计 30 秒；切换到另一首后重新计数。本版不提供总收听时长、实时在线状态、完整留存或实名好友列表。

播放进度与经过时间交叉检查；暂停、缓冲、静音、跳转进度和超过 5 秒的迟到进度回调不累计。后台回调节流可能少计，不能解释成精确听歌时长；播放器开始播放也不能证明设备实际出声或用户听见。

## 数据字段

- 事件编号 event_id：随机 UUID，接收端用于去重。
- 事件类型 event：以上三个白名单值。
- 发生时间 occurred_at：浏览器 UTC 时间，可能受设备时钟误差影响；接收端另记接收时间。
- 匿名访客 visitor_id：随机编号，保存在当前浏览器；清理缓存、换浏览器或设备会变化，不等于真实人数。
- 本次页面 session_id：每次页面载入生成，不是行业标准的 30 分钟会话。
- 来源 source：只接受 share、wechat、friend，其余统一 direct；不采集完整 URL 或 referrer。
- 歌曲 track_id、track_title、系列 series_id：只来自本地曲库；打开事件留空。
- 版本 version：1。

不采集工作文字、搜索词、姓名、联系方式。关闭统计后停止新事件并移除本地访客编号；已收到的历史数据不会自动删除。尊重浏览器 DNT/GPC 信号。接收端未配置或离线打开时不创建统计标识，也不发送请求。

## 飞书待办与验收

1. 恢复飞书用户登录授权后，新建“慢慢书桌 · 使用统计”多维表格，表名“访问与播放记录”。配置上述字段和接收时间。
2. 在用户账号内核实免费额度、自动化执行次数和表格容量。CLI 当前工作流结构不列出 Webhook 触发器，需要在飞书页面配置并验证。
3. 验证浏览器到飞书入口的请求能力。公网前端不能包含飞书应用密钥、用户凭据或私有 webhook；如需中转接收接口，完成部署、白名单字段校验、长度限制、歌曲目录校验、事件去重和限流后再开启采集。Origin 限制本身不构成身份验证。
4. 使用标为测试来源的数据完成页面打开→播放→30 秒→飞书实际新增记录的验证；测试数据与真实数据分开。当前前端不发送测试数据，实际验证时由接收端标记测试运行。
5. 仪表盘至少包含每天打开次数、匿名浏览器数、开始播放人数、30 秒收听次数、歌曲排行和来源分布。默认歌曲曝光较高，排行不能直接当成喜好排名。
6. 确认前述链路通过后填写公开采集接口地址，并显示匿名统计说明与关闭入口；再发布网站。未完成之前不可声称已有真实访问统计。

## 费用与容量

初版每次打开约 1 条访问记录，每次选曲最多 2 条播放记录；不按秒上报。实际自动化消耗还要以飞书流程计费口径为准。配额在用户账号内核实，不能仅凭免费版支持 Webhook 承诺无限免费。
