# 慢慢书桌 · 匿名使用统计

当前状态：已改为 Supabase Edge Functions 接收并保存到 Postgres，私人统计页需要管理员登录。飞书不再参与数据接收。接口测试已通过；网站本次发布后启用匿名统计。手机微信已确认 Supabase 地址可达；Cloudflare 默认地址不可达，旧采集入口停用。

## 第一版口径

| 事件 | 触发方式 | 用途 |
|---|---|---|
| page_view | 开启统计后，每次页面载入一次 | 打开次数与时间 |
| play_start | 选中音乐实际开始播放且应用音量大于零 | 有效开始播放 |
| listen_30s | 同一次选曲累计有效媒体进度达到 30 秒 | 有效收听 |

一首歌曲暂停后恢复不重复计播放开始，同一选曲循环不重复计 30 秒；切换到另一首后重新计数。本版不提供总收听时长、实时在线状态、完整留存或实名好友列表。

播放进度与经过时间交叉检查；暂停、缓冲、静音、跳转进度和超过 5 秒的迟到进度回调不累计。后台回调节流可能少计，不能解释成精确听歌时长；播放器开始播放也不能证明设备实际出声或用户听见。

## 数据字段

- 事件编号 event_id：随机 UUID，接收端用于去重。
- 事件类型 event：以上三个白名单值。
- 发生时间 occurred_at：浏览器 UTC 时间，可能受设备时钟误差影响；接收端另记接收时间。
- 匿名访客 visitor_id：随机编号，保存在当前浏览器；清理缓存、换浏览器或设备会变化，不等于真实人数。
- 本次页面 session_id：每次页面载入生成，不是行业标准的 30 分钟会话。
- 来源 source：只接受 share、wechat、friend，其余统一 direct；不采集完整 URL 或 referrer。
- 歌曲 track_id、track_title、系列 series_id：只来自本地曲库；打开事件留空。
- 版本 version：1。

不采集工作文字、搜索词、姓名、联系方式。关闭统计后停止新事件并移除本地访客编号；已收到的历史数据不会自动删除。尊重浏览器 DNT/GPC 信号。接收端未配置或离线打开时不创建统计标识，也不发送请求。

## 统计后台与存储

公开统计页显示近 7 天或 30 天的打开次数、匿名访客数、开始播放、听满 30 秒、每日趋势、歌曲排行、来源分布和最近 50 条事件。任何拿到 `/stats.html` 网址的人都可免密码查看。时间统一按北京时间；默认排除测试数据，也可切换为只看测试数据。不能识别具体朋友，不能据此判断实时在线状态。

匿名事件保存在 Supabase 独立统计表，接收时间超过 62 天后定时清理。不保存 IP、完整 URL、搜索词或文本输入。公开概览免登录读取，但原始数据库表和逐人明细接口仍受保护。旧管理员密钥的 SHA-256 摘要留在数据库，原密钥只在本机私有文件中，不写入 Git；公开统计页不再使用密钥。

## 费用、限流和上线条件

每天最多接受 1,000 条事件，每秒最多 5 条、每分钟最多 30 条；重复事件不重复保存。每次选曲最多 2 条播放事件，不按秒上报。浏览器拦截、网络不可达和限流都会导致漏记，统计失败不阻断音乐播放。免费方案受 Supabase 免费项目总额度限制，不能承诺无限使用。

已验证手机项目域名可达、模拟测试事件落库、公开概览免登录可读、真实数据排除测试记录。前端原有打开/播放/30 秒事件逻辑通过回归测试；本轮未宣称完成手机真实播放上报的全流程操作。发布时同步显示网页匿名说明和关闭入口。原网站 Git 回滚标签为 `rollback/before-cloudflare-analytics-20260920`；停用时清空 endpoint 并关闭接收端额度即可，无需删除历史数据。
